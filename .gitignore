# -------------------------
# 🖥 운영체제(OS) 불필요 파일 무시
# -------------------------
.DS_Store       # macOS 자동생성 파일
Thumbs.db       # Windows 미리보기 캐시 파일

# -------------------------
# 🔑 환경 설정 및 보안 정보
# -------------------------
.env            # API Key, DB 정보 등 포함 - 절대 업로드 금지
.env.*          # 다양한 env 파일 패턴 포함
*.log           # 로그 파일

# -------------------------
# 🐍 Python (FastAPI) 관련
# -------------------------

# 파이썬 바이트코드 / 빌드 캐시
__pycache__/
*.py[cod]
*$py.class

# 가상환경(업로드 불필요)
venv/
env/
.venv/
.pdm/

# 패키징 및 배포 파일
build/
dist/
*.egg-info/
.eggs/
.wheels/

# SQLite 등 개발용 데이터베이스 파일 (공유 불필요)
*.sqlite
*.sqlite3

# 테스트 / 커버리지 파일
.coverage
htmlcov/
.tox/
pytest_cache/
coverage.xml

# IDE/에디터 자동 생성 파일
.idea/          # JetBrains (PyCharm)
.vscode/        # VSCode
*.swp           # Vim 임시파일

# -------------------------
# 📱 Flutter 프로젝트 관련
# -------------------------

# Flutter 빌드 결과물 (공유 불필요)
build/
.dart_tool/
.packages

# Flutter 플러그인 관련 자동 생성 파일
.flutter-plugins
.flutter-plugins-dependencies
generated_plugin_registrant.dart

# pubspec.lock는 팀 상황에 따라 선택 (패키지 버전 고정 원하면 포함)
pubspec.lock

# iOS / Android 빌드 결과물 (자동 생성됨)
ios/Pods/
ios/.symlinks/
ios/build/
ios/.generated/
android/.gradle/
android/build/
android/app/build/

# 크래시 로그 / 심볼 파일
*.symbols
*.lock

# -------------------------
# 📦 JS 환경을 사용한다면 (선택)
# -------------------------
node_modules/   # JS 패키지가 있을 경우 자동 생성됨

# -------------------------
# 🐳 Docker (선택)
# -------------------------
docker-volume/   # 로컬 볼륨 데이터 무시
