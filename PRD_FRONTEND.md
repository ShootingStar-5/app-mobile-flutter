# 약꼬박 (Yakkobak) Flutter 앱 - PRD (Product Requirements Document)

## 1. 개요 (Overview)

**제품명**: 약꼬박 (Yakkobak) - 약봉투 촬영으로 복약 알람 자동 설정  
**플랫폼**: Android (Flutter)  
**목적**: 고령층을 위한 간편한 약 복용 알람 앱으로, 약봉투 사진 촬영만으로 알람이 자동 설정되는 서비스  
**대상 사용자**: 약 복용이 필요한 모든 연령층, 특히 고령층  
**핵심 가치**: "찰칵! 하나로 약 알람 완성"

## 2. 핵심 기능 (Core Features)

### 2.1 약봉투 촬영 및 OCR 분석
- **카메라 통합**: Flutter camera 플러그인 활용
- **사진 촬영**: 약봉투를 카메라로 촬영
- **백엔드 전송**: 이미지를 FastAPI 백엔드로 전송
- **자동 분석**: OCR + LLM을 통한 약 정보 자동 추출
- **결과 확인**: 추출된 약 정보 표시 및 수정 가능

### 2.2 자동 알람 생성
- **복약 스케줄 자동 계산**:
  - 복용 기간 (예: 3일, 5일, 7일)
  - 일 복용 횟수 (예: 1일 3회)
  - 식사 기준 (식전/식후/취침전)
  - 시간 간격 (예: 식후 30분)
- **알람 시간 자동 설정**:
  - 아침: 08:00 + 오프셋
  - 점심: 12:00 + 오프셋
  - 저녁: 18:00 + 오프셋
  - 취침전: 22:00
- **기간 기반 반복 설정**: 복용 기간만큼 알람 반복

### 2.3 알람 관리
- **알람 목록 화면**:
  - 현재 활성화된 알람 목록 표시
  - 알람 시간, 약 이름, 식사 기준 표시
  - 활성화/비활성화 토글
- **알람 수정**:
  - 시간 변경
  - 약 정보 수정
  - 삭제 기능
- **알람 울림**:
  - 전체 화면 알람 (앱이 꺼져있어도 동작)
  - 알람음 재생
  - 스누즈/확인 버튼

### 2.4 음성 입력 (STT)
- **음성으로 약 정보 입력**:
  - Azure Speech Service 활용
  - "하루 세 번, 식후 30분, 3일분" 같은 자연어 인식
  - OCR 보완 수단으로 활용
- **음성 명령**:
  - "알람 추가"
  - "알람 확인"

### 2.5 안심 알람 (추후 구현)
- 약을 먹지 않았을 때 보호자에게 알림
- 복약 기록 저장 및 통계

## 3. UI/UX 요구사항 (UI/UX Requirements)

### 3.1 디자인 원칙
- **고령층 친화적 UI**:
  - 큰 폰트 크기 (최소 16pt)
  - 명확한 아이콘과 버튼
  - 단순한 네비게이션 (최대 2단계 깊이)
- **고대비 컬러**:
  - 텍스트와 배경의 명확한 구분
  - 중요 버튼은 높은 채도 색상
- **터치 친화적**:
  - 버튼 최소 크기: 48x48dp
  - 충분한 여백

### 3.2 화면 구조

#### 3.2.1 홈 화면 (AlarmScreen)
```

```

#### 3.2.2 약봉투 촬영 화면 (CameraScreen)
```
┌─────────────────────────┐
│  [ ← ]     약봉투 촬영   │
├─────────────────────────┤
│                         │
│    [카메라 프리뷰]       │
│                         │
│    ┌─────────────┐      │
│    │ 약봉투를    │      │
│    │ 이곳에 위치 │      │
│    └─────────────┘      │
│                         │
│                         │
│      [ 📸 촬영 ]        │
│                         │
│ [갤러리]                │
└─────────────────────────┘
```

#### 3.2.3 약 정보 확인 화면 (MedicationEditScreen)
```
┌─────────────────────────┐
│  [ ← ]   약 정보 확인    │
├─────────────────────────┤
│                         │
│  약 이름                │
│  [ 감기약          ]    │
│                         │
│  복용 기간              │
│  [ 3일             ]    │
│                         │
│  일 복용 횟수           │
│  [ 3회             ]    │
│                         │
│  식사 기준              │
│  ( ) 식전  (●) 식후     │
│  ( ) 취침전             │
│                         │
│  시간 간격              │
│  [ 30분            ]    │
│                         │
│  [ 알람 생성 ]          │
└─────────────────────────┘
```

#### 3.2.4 알람 울림 화면 (AlarmRingingScreen)
```
┌─────────────────────────┐
│                         │
│      🔔                 │
│                         │
│   약 먹을 시간이에요!    │
│                         │
│   감기약                │
│   식후 30분            │
│                         │
│   [ 스누즈 (10분) ]     │
│                         │
│   [ ✓ 복용 완료 ]       │
│                         │
└─────────────────────────┘
```

### 3.3 컬러 팔레트


### 3.4 타이포그래피
- **헤더**: 24sp, Bold
- **본문**: 18sp, Regular
- **버튼**: 20sp, Medium
- **캡션**: 14sp, Regular

## 4. 사용자 플로우 (User Flow)

### 4.1 메인 플로우: 약봉투 촬영 → 알람 생성
```
1. 홈 화면 진입
    ↓
2. "약봉투 촬영" 버튼 클릭
    ↓
3. 카메라 권한 요청 (첫 실행 시)
    ↓
4. 카메라 화면 진입
    ↓
5. 약봉투를 카메라에 맞추고 촬영
    ↓
6. 이미지 백엔드로 전송 (로딩 표시)
    ↓
7. OCR + LLM 분석 (3-5초)
    ↓
8. 약 정보 확인 화면 표시
    ↓
9. 사용자 정보 확인 및 수정 (필요시)
    ↓
10. "알람 생성" 버튼 클릭
    ↓
11. 알람 자동 생성 (복용 기간 × 복용 횟수)
    ↓
12. 홈 화면으로 복귀 (생성된 알람 목록 표시)
```

### 4.2 서브 플로우: 음성 입력
```
1. "음성으로 입력" 버튼 클릭
    ↓
2. 마이크 권한 요청 (첫 실행 시)
    ↓
3. 음성 녹음 시작
    ↓
4. "하루 세 번, 식후 30분, 3일분" 발화
    ↓
5. STT 변환 및 백엔드 전송
    ↓
6. LLM 분석
    ↓
7. 약 정보 확인 화면 (이후 동일)
```

### 4.3 서브 플로우: 알람 수정/삭제
```
1. 홈 화면에서 알람 카드 클릭
    ↓
2. 알람 상세 화면
    ↓
3. 수정 또는 삭제 선택
    ↓
4. 변경 사항 저장
```

## 5. 기술 요구사항 (Technical Requirements)

### 5.1 개발 환경
- **Flutter SDK**: 3.x
- **Dart**: 3.x
- **Android**: minSdkVersion 21 (Android 5.0+)
- **개발 도구**: VSCode 또는 Android Studio

### 5.2 주요 플러그인
```yaml
dependencies:
  flutter:
    sdk: flutter
  
  # 상태 관리
  provider: ^6.0.0
  
  # 로컬 저장소
  shared_preferences: ^2.0.0
  
  # 카메라
  camera: ^0.10.0
  image_picker: ^1.0.0
  
  # HTTP 통신
  http: ^1.0.0
  
  # 알람
  alarm: ^3.0.0
  flutter_local_notifications: ^17.0.0
  
  # 음성 인식 (추후)
  speech_to_text: ^6.0.0
  
  # UI
  intl: ^0.18.0
```

### 5.3 아키텍처

#### 5.3.1 디렉토리 구조
```
lib/
├── main.dart                 # 앱 진입점
├── models/                   # 데이터 모델
│   └── alarm.dart
├── screens/                  # 화면
│   ├── alarm_screen.dart         # 홈/알람 목록
│   ├── camera_screen.dart        # 카메라 촬영
│   ├── medication_edit_screen.dart  # 약 정보 확인/수정
│   └── alarm_ringing_screen.dart    # 알람 울림
├── services/                 # 비즈니스 로직
│   ├── api_service.dart          # 백엔드 API 통신
│   ├── alarm_service.dart        # 알람 관리
│   └── notification_service.dart # 알림 관리
└── widgets/                  # 재사용 위젯
    ├── alarm_card.dart
    └── custom_button.dart
```

#### 5.3.2 상태 관리
- **Provider 패턴** 사용
- 알람 상태를 전역으로 관리
- 화면 간 데이터 공유

### 5.4 백엔드 통신

#### 5.4.1 API 서비스 (api_service.dart)
```dart
class ApiService {
  static const String baseUrl = 'http://your-server.com';
  
  Future<MedicationData> analyzeMedicationImage(File image) async {
    var request = http.MultipartRequest(
      'POST',
      Uri.parse('$baseUrl/api/v1/ocr/extract-medication'),
    );
    request.files.add(await http.MultipartFile.fromPath('file', image.path));
    
    var response = await request.send();
    var responseData = await response.stream.toBytes();
    var result = json.decode(String.fromCharCodes(responseData));
    
    return MedicationData.fromJson(result['medication_data']);
  }
}
```

#### 5.4.2 데이터 모델 (models/alarm.dart)
```dart
class MedicationData {
  final String medicationName;
  final int totalDurationDays;
  final int dailyFrequency;
  final String mealContext;
  final int specificOffsetMinutes;
  final String? specialInstructions;

  MedicationData({
    required this.medicationName,
    required this.totalDurationDays,
    required this.dailyFrequency,
    required this.mealContext,
    required this.specificOffsetMinutes,
    this.specialInstructions,
  });

  factory MedicationData.fromJson(Map<String, dynamic> json) {
    return MedicationData(
      medicationName: json['medication_name'],
      totalDurationDays: json['total_duration_days'],
      dailyFrequency: json['daily_frequency'],
      mealContext: json['meal_context'],
      specificOffsetMinutes: json['specific_offset_minutes'],
      specialInstructions: json['special_instructions'],
    );
  }
}
```

### 5.5 알람 생성 로직

```dart
void createAlarmsFromMedication(MedicationData data) {
  DateTime now = DateTime.now();
  
  // 기본 시간 설정
  Map<int, int> mealTimes = {
    0: 8,   // 아침
    1: 12,  // 점심
    2: 18,  // 저녁
  };
  
  for (int day = 0; day < data.totalDurationDays; day++) {
    for (int freq = 0; freq < data.dailyFrequency; freq++) {
      DateTime alarmTime = DateTime(
        now.year,
        now.month,
        now.day + day,
        mealTimes[freq]!,
        data.specificOffsetMinutes,
      );
      
      // 알람 생성
      Alarm.set(
        alarmSettings: AlarmSettings(
          id: generateId(),
          dateTime: alarmTime,
          assetAudioPath: 'assets/alarm.mp3',
          notificationTitle: data.medicationName,
          notificationBody: '${getMealContextText(data.mealContext)} 복용 시간입니다',
        ),
      );
    }
  }
}
```

### 5.6 권한 관리
- **카메라**: 약봉투 촬영
- **저장소**: 사진 저장 (선택)
- **마이크**: 음성 입력 (추후)
- **알림**: 알람 푸시
- **전체 화면**: 알람 울림 화면

## 6. 성능 요구사항 (Performance Requirements)

### 6.1 앱 성능
- **앱 시작 시간**: 2초 이내
- **카메라 로딩**: 1초 이내
- **API 응답 대기**: 로딩 인디케이터 표시
- **알람 정확도**: ±10초 이내

### 6.2 네트워크
- **이미지 전송**: 최대 4MB
- **압축**: 필요시 이미지 리사이징
- **타임아웃**: 30초
- **재시도**: 실패 시 최대 3회

### 6.3 배터리 최적화
- 백그라운드 작업 최소화
- 알람만 백그라운드에서 동작
- 불필요한 위치 추적 없음

## 7. 보안 및 개인정보 (Security & Privacy)

### 7.1 데이터 수집
- **최소 수집 원칙**: 약 정보만 수집
- **사진 저장**: 사용자 선택권 제공 (기본: 저장 안 함)
- **로컬 저장**: 알람 정보는 기기에만 저장

### 7.2 네트워크 보안
- **HTTPS 통신**: 모든 API 요청
- **API 키**: 환경 변수로 관리
- **인증**: 필요시 JWT 토큰 사용

## 8. 테스트 계획 (Testing Plan)

### 8.1 단위 테스트
- 데이터 모델 파싱
- 알람 생성 로직
- API 통신 모킹

### 8.2 통합 테스트
- 카메라 → API → 알람 생성 플로우
- 음성 → API → 알람 생성 플로우

### 8.3 사용성 테스트
- 고령층 사용자 테스트 (5명 이상)
- 피드백 수집 및 개선

## 9. 배포 계획 (Deployment Plan)

### 9.1 버전 관리
- **버전 1.0.0**: 기본 기능 (OCR + 알람)
- **버전 1.1.0**: 음성 입력 추가
- **버전 1.2.0**: 안심 알람 기능
- **버전 2.0.0**: UI/UX 대폭 개선

### 9.2 배포 플랫폼
- **Google Play Store**: Android 앱 배포
- **베타 테스트**: Google Play 내부 테스트 트랙

### 9.3 출시 전 체크리스트
- [ ] 모든 권한 설명 추가
- [ ] 개인정보 처리방침 작성
- [ ] 서비스 이용약관 작성
- [ ] 앱 아이콘 제작
- [ ] 스크린샷 준비 (Play Store용)
- [ ] 앱 설명 작성 (한글/영문)

## 10. 향후 개선 방향 (Future Enhancements)

### 10.1 단기 (1-2개월)
- [ ] 음성 입력 기능 완성
- [ ] 복약 기록 저장 및 캘린더 뷰
- [ ] 약 정보 데이터베이스 구축
- [ ] 오프라인 모드 지원

### 10.2 중기 (3-6개월)
- [ ] 안심 알람 (보호자 알림)
- [ ] 약 상호작용 경고
- [ ] 복약 통계 및 리포트
- [ ] iOS 버전 개발

### 10.3 장기 (6개월 이상)
- [ ] AI 챗봇 복약 상담
- [ ] 처방전 자동 인식
- [ ] 약국 연동 서비스
- [ ] 건강보험공단 연동

## 11. 성공 지표 (Success Metrics)

### 11.1 정량적 지표
- **월간 활성 사용자 (MAU)**: 1,000명 이상
- **알람 정확도**: 95% 이상
- **OCR 성공률**: 85% 이상 (백엔드 기준)
- **앱 평점**: 4.5점 이상
- **리텐션**: 1개월 30% 이상

### 11.2 정성적 지표
- 고령층 사용자 만족도
- 사용 편의성 피드백
- 버그 및 크래시 최소화

---

**문서 버전**: 1.0  
**최종 수정일**: 2024년 12월 1일  
**작성자**: Yakkobak 개발팀
